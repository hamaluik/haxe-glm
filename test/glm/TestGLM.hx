package glm;

import buddy.*;
import glm.GLM;
using buddy.Should;

class TestGLM extends BuddySuite {
	public function new() {
		describe('Using GLM', {
			it('should be able to create a frustum matrix', {
			});
		});
	}
}